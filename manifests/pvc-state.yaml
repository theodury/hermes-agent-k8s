apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hermes-agent-state
  namespace: ai
  labels:
    app: hermes-agent
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path-data
  resources:
    requests:
      storage: 20Gi
